generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model CoffeeLog {
  id          Int      @id @default(autoincrement())
  timestamp   DateTime @default(now())
  caffeine_mg Float
  coffee_type String?

  @@map("coffee_logs")
}
