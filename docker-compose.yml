services:
  coffee-tracker:
    build: .
    container_name: coffee-tracker
    ports:
      - "4000:4000"
    environment:
      - DATABASE_URL=sqlite:////app/data/coffee.db
      - API_KEY=${API_KEY}
      - LOG_LEVEL=${LOG_LEVEL}
    volumes:
      - ./data:/app/data
    restart: unless-stopped